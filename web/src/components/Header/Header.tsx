import * as React from "react";
import "./Header.css";

export const Header: React.FunctionComponent = () => {
  return (
    <header className="header">
      <h2>Hooks(⚓) use</h2>
      <h4>Github</h4>
    </header>
  );
};
